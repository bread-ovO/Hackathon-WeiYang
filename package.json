{
  "name": "bugu",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.34.5",
  "engines": {
    "node": ">=22.12.0 <25"
  },
  "scripts": {
    "dev": "pnpm --filter @memo/desktop dev",
    "build": "pnpm --filter @memo/desktop build",
    "typecheck": "tsc --noEmit",
    "check:boundaries": "node scripts/check-boundaries.mjs",
    "test": "vitest run",
    "test:storage": "node scripts/test-storage.mjs",
    "test:desktop": "playwright test",
    "rebuild:native": "pnpm --filter @memo/desktop rebuild:native",
    "check": "node scripts/check-live2d-boundary.mjs && pnpm check:boundaries && pnpm typecheck && pnpm test && pnpm build && pnpm test:storage && pnpm test:desktop",
    "package:dir": "pnpm --filter @memo/desktop package:dir"
  },
  "devDependencies": {
    "@playwright/test": "1.58.2",
    "@types/node": "22.19.11",
    "@types/react": "19.2.14",
    "@types/react-dom": "19.2.3",
    "@types/better-sqlite3": "7.6.13",
    "typescript": "5.9.3",
    "vitest": "3.2.4",
    "esbuild": "0.25.12"
  }
}
