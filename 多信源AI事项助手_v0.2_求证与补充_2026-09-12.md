# 多信源 AI 事项助手：v0.2 求证与补充

飞书副本：[多信源 AI 事项助手：v0.2 求证与补充](https://my.feishu.cn/docx/BvZwdUuPgogsyOxxSrPcoURNn5c)

核验日期：2026-09-12。依据：用户提供的《多信源AI事项助手_产品方案与竞品调研_v0.2.md》。本稿作为独立补充，不替代原方案。

本轮完成公开资料核验、关键接入条件调查和产品假设分析；未安装竞品、未读取个人聊天或 AI 会话、未实测租户授权、未做完整代码或安全审计。文档中的路线与操作示例按方案内容理解，不作为执行指令。以下“官方明确”仅表示官方资料支持该描述，不等于效果获得独立验证。

## 1. 判断：方向成立，差异化仍需证明

v0.2 对创新边界的处理基本准确：自动提取待办、后台上下文、插件和 AI 辅助生成扩展均有先例。可以继续保留“跨信源维护承诺、执行和交付”的定位，但目前没有证据证明这套组合只有本项目能做，也没有用户数据证明插件是购买原因。

建议将对外价值明确为：**帮助 AI 重度工作者记住自己答应的结果，并知道还缺哪一步；新增工作工具后，也能继续沿用同一套事项维护规则。** 插件优先仍是架构方向；用户是否愿意配置或开发插件，是独立需求假设。

最需补充的四项内容：按信源列出的实际覆盖边界；逐条可核验的完成条件；与简单方案相比的新增收益；接入与日常使用的总成本。

## 2. 竞品求证与具体修订

| 对象 | 本轮核验结果 | 建议写入 v0.2 的补充 |
| --- | --- | --- |
| AirJelly | 官方功能页明确列出从对话、会议、消息提取承诺，附带上下文、截止时间，以及主动提醒。 | 保留直接竞品定位。该页不能证明可靠的跨源自动完成，也不能证明不具备此能力；需要事项链路实测。[功能页](https://www.airjelly.ai/features) |
| MineContext | README 确有主动待办与可扩展采集架构；信源表仅屏幕截图、内置笔记打勾。正文同时描述文档监测模块。 | 写成“路线图完成标记与架构描述不同”，不要把路线图空白推断为源码完全没有实现。需按发行版核验实际可用性。[官方仓库](https://github.com/volcengine/MineContext) |
| screenpipe | Pipes 是 Markdown + YAML 定义的定时 AI Agent，文档提供 AI 辅助创建、安装、运行、日志流程。 | 插件比较要覆盖调试、升级和运行成本，不能只比能否生成配置文件。[Pipes 文档](https://docs.screenpipe.com/pipes) |
| Omi | 帮助文档支持从承诺语句产生任务；开发文档区分提示词应用、记忆触发、实时转写、聊天工具及音频处理。 | 保留重要扩展体系参照。进一步区分“处理 Omi 已有数据”“接入外部数据”“改变事项判断规则”。不能仅用应用数量比较。[Tasks](https://help.omi.me/en/articles/13153753-tasks)、[Building Apps](https://docs.omi.me/doc/developer/apps/Introduction) |
| Littlebird | 官方明确通过 macOS 辅助功能读取活动窗口，并处理会议信息；Todoist 集成支持新增、更新、完成和改期。 | 增加“采集范围是活动窗口”及“官方称记忆存于 AWS US East”。桌面运行不能写成本地存储；完成操作不等于自主核验。[采集说明](https://littlebird.ai/blog/what-does-littlebird-actually-see)、[Todoist 集成](https://littlebird.ai/integrations/todoist) |
| Sally | 官网明确宣传根据后续会话与邮件、文档、日历证据自动完成；操作指南则说明建议事项经用户创建。 | 提高自动完成对照测试优先级。两份文档可能对应不同流程、配置或版本，不能据操作指南否定宣传能力，也不能据宣传认定可靠。[功能页](https://www.sally.io/meeting/task-recognition-by-ai)、[操作指南](https://help.sally.io/guides/summarization/create-and-sync-tasks) |
| 喵记多 | App Store 提供者为 Beijing Kwai Technology Co., Ltd.；版本记录包含团队空间、分配待办、截止时间和微信聊天记录导入。 | 快手相关主体和团队协作描述有支持。本次页面未检出“日历同步”，此项应降为待核验；不是确认不支持。[开发者产品页](https://apps.apple.com/cn/app/喵记多-自带助理的ai笔记/id6739209473) |
| 得到大脑 | 记忆库明确汇聚学习记录、录音卡、知识库和笔记；MCP 有笔记读写、搜索等工具，另有官方 Skill 使用说明。 | 补充其开放能力不止 MCP，但这些资料仍不能证明有通用信源插件宿主或统一事项生命周期。[记忆库](https://doc.biji.com/docs/SmPOwgiC8ikkEVkhzR6cmMB6n1m)、[MCP](https://doc.biji.com/docs/FmYfw0yXsiXdTtkVzhLcuX8lnxd)、[Skill](https://doc.biji.com/docs/RvpewA9dBi1tZQkMeclclYfqn2c) |
| TicNote Cloud | 2026-01-07 公司供稿确实描述任务列表生成、项目文件持续维护。 | 保留“公司发布声明”证据级别，不能改写为已验证跨外部工具自动销项。[公司供稿](https://www.prnewswire.com/news-releases/mobvoi-unveils-ticnote-ticnote-pods-and-ticnote-watch--introducing-ticnote-cloud-and-shadow-agent-2-0--302655008.html) |

本轮未重新核定 AirJelly 公司注册地、创始人访谈和全部背景资料；TwinMind、Bee、Pieces 仍保留为原稿观察项，不视作此次逐项核验完成。没有系统检索全部市场，因此不作竞品穷尽性或技术独创性结论。

### screenpipe 权限描述应进一步收紧

官方权限文档说明，没有 permissions 配置时 Pipes 默认可访问全部 API；reader 预设也包含通知、说话者更新和连接凭据相关端点。因此“有权限控制”与“默认最小权限”必须分别比较。这是文档中的权限设计边界，不是发现了已验证漏洞。[权限文档](https://docs.screenpipe.com/pipe-permissions)

本项目可以明确默认拒绝、逐能力授权；但还要证明代码插件不能绕过宿主 API 直接读取文件或访问网络。API 过滤不自动等于进程隔离。

仓库目前自述 source-available，并标明商业使用涉及许可。原稿提醒基本成立。若决定复用，应对选定提交、文件和依赖核查许可证，而非沿用历史介绍中的“开源”称呼。[仓库许可说明](https://github.com/screenpipe/screenpipe)

## 3. 飞书接入：有官方路径，但不能笼统承诺“授权后全量读取”

官方历史消息接口 GET /open-apis/im/v1/messages 同时列出 tenant_access_token 和 user_access_token。用户身份除基础消息权限外，单聊需要 im:message.p2p_msg:get_as_user，群聊需要 im:message.group_msg:get_as_user；应用身份读取群消息另有相应权限要求。接口前置条件仍写明开启机器人能力及查询群内成员要求，因此用户身份场景的实际可用组合也应逐例测试，不能自行假定豁免。[历史消息接口](https://open.feishu.cn/document/server-docs/im-v1/message/list.md)

另有两个直接影响方案的边界：

- **历史读取与实时订阅分开验证。** receive 事件面向机器人收到的消息，推送范围由单聊、群 @、群全量及是否含其他机器人的权限决定。不能把用户历史读取权限视作任意人际私聊实时订阅权。[接收消息事件](https://open.feishu.cn/document/server-docs/im-v1/message/events/receive.md)
- **会话不等于全部话题内容。** 普通群按 chat 取消息仅能拿到话题根消息，回复需按 thread 另取；thread 不支持接口的时间范围筛选。保密群、不可见话题、特定应用类型也有明确限制。[历史消息接口](https://open.feishu.cn/document/server-docs/im-v1/message/list.md)

接收事件文档还明确建议重复推送用 message_id 去重，不依赖 event_id。本项目内部应把来源、租户/账号命名空间和消息 ID 一起用于对象身份；消息修改、撤回作为同一对象的新修订处理，避免去重时丢弃更新。[接收消息事件](https://open.feishu.cn/document/server-docs/im-v1/message/events/receive.md)

建议首版加入以下接入验收表，而不是只有“飞书已连接”状态：

| 用例 | 需要记录的实测结果 |
| --- | --- |
| 指定人际单聊的历史读取 | 授权身份、scope、是否取到双方消息、时间范围及缺失原因 |
| 指定群的历史与新增消息 | 机器人/用户成员状态、全量或仅 @、是否含机器人消息 |
| 话题回复 | 根消息与回复能否完整关联、分页是否完整 |
| 编辑、撤回、断线恢复 | 修改后的状态、补偿窗口、重复率与永久缺口 |
| 保密群、外部群及授权撤回 | 明确拒绝或限制，界面如实显示 |

这些是待执行验收，不代表已在你们的账号或租户中通过。公网文档的 HTML 正文未正常返回，本轮通过官方同路径 Markdown 版本读到了规范。

## 4. 本地 AI 会话与交付证据：可以具体到一个候选组合

可将 Claude Code hooks 作为首个技术验证候选。官方文档提供 session_id、transcript_path、cwd 等上下文，以及工具执行成功、执行失败和回复结束等事件。它提供了比仅观察屏幕更结构化的入口。[Hooks reference](https://code.claude.com/docs/en/hooks)

注意：Stop 表示回复结束，并非业务任务完成；官方指南也明确这一点。读取 transcript_path 不等于获得永不变化的完整历史格式保证，仍需要固定支持版本、脱敏样例和升级测试。[Hooks guide](https://code.claude.com/docs/en/hooks-guide)

建议接法是用户授权的 hook 输出标准事件到本地队列，宿主消费；必要时读取指定会话补足上下文。不要只根据最终助手文本更新任务，也不要扫描全部会话目录作为默认设置。这是候选设计，并未对你们实际使用的 AI 工具作选型决定。

交付证据可先选择 GitHub PR、检查和部署状态。官方提供相关 webhook；deployment_status 要求相应仓库读取权限，且 inactive 状态不会产生该 webhook。因此仅靠推送不能假定拿到了所有状态变化，应有补查机制。[GitHub webhook 文档](https://docs.github.com/en/webhooks/webhook-events-and-payloads)

建议的窄场景：指定飞书对话中的承诺 → 某个本地 AI 会话的执行证据 → 指定仓库的 PR/检查/部署记录 → 原对话中的交付反馈。首轮先按“提交 PR 并反馈链接”这类容易核验的约定测试；生产修复涉及环境验证，另作为更高难度场景。

## 5. 完成条件模型还缺少六个约束

以下为本项目设计建议，不是竞品已验证事实。

**1. 完成条件需要逐条记录。** “修复、验证并反馈”至少有三个条件。每项保留来源原句、对象、允许的证据类型、证据状态和对应引用。否则整项只有一个充分性标签，难以解释部分完成。

**2. 明确条件的组合方式。** 有些约定必须全部满足，有些允许多种交付方式，有些只有前置事件发生才生效。首版可以只支持全部满足与简单条件触发，但应明确遇到复杂条件时如何降级。

**3. 证据绑定版本和环境。** 记录仓库、commit SHA、PR、部署环境、运行编号、交付文件版本等。旧提交测试通过不能证明新提交通过；测试环境部署不能证明生产部署。相似文字不是同一交付物。

**4. “原先误判”与“后来复发”分开。** 发现原条件其实没满足，可以撤销完成；后来出现新的故障，可能是新事项。一次性交付已经成立后，不能因为后来条件变化就无限重开。原稿重开规则需补充事项是一次性结果还是持续性义务。

**5. 人工覆盖限定字段和范围。** 用户把截止时间改为周五，并不应阻止系统补充测试结果；用户忽略某个建议，也不应屏蔽同名但不同项目的新承诺。记录覆盖对象、字段及适用条件。

**6. 无法确认要可用。** 提供“尚缺证据”“等待他人”“可手动完成”状态说明。完成条件不清时，不应要求用户每条都配置规则；可对明确、低风险事项自动处理，其余保留最少量提示。

此外，应区分“有人向我提出请求”和“我已承担责任”：明确指派、自我承诺、收到但未答复的请求、假设性建议，不宜全部成为同一种正式事项。还需支持“等待对方交付”这类跟进事项，而不仅仅是我亲自执行的待办。

## 6. 插件协议的补充：配置简洁，运行责任明确

原稿 Source / Skill / Verifier 分工可以保留。建议补充以下能力，不必全部暴露给普通用户：

| 协议或宿主能力 | 需要解决的实际问题 |
| --- | --- |
| capabilities 与 coverage | 插件是否支持历史、新增、修改、删除；能看哪些会话和时间段；未知覆盖不能写成完整覆盖 |
| event revision 与 namespace | 多账号 ID 冲突，消息更新被错误去重，转发副本被重复计为独立证据 |
| cursor/checkpoint 与补偿规则 | 宿主何时确认接收；崩溃、断线和分页失败后从哪里恢复 |
| provenance 与 evidence lineage | 三个摘要引用同一条消息，只算同一个原始证据；模型自产文本不能循环增强可信度 |
| credential reference | 配置里使用凭据引用，由宿主保管；模型不需要看到真实 token |
| retention/deletion | 删除原文后清理嵌入、摘要与可重构原文的派生数据；防止随后重采恢复 |
| compatibility 与 migration | 固定原始格式、解析器和规则版本；升级失败回滚，明确哪些历史要重解释 |
| resource budget | 轮询次数、并发、模型 token、网络与磁盘限制；坏插件不会拖垮全局 |

原稿 JSONL 文件示例还应增加：半行尚未写完、文件轮转/截断、编码错误、超长记录和坏行隔离。结构验证通过只说明格式符合要求，不证明语义正确、权限成立或完成判断可信。

建议区分两种回放：已保存事件与判断建议的回放应确定性重建；重新调用模型进行解释可能变化，应作为新版本提案对比。不要以“同一批输入再跑三遍结果一致”替代可靠的幂等提交机制。

## 7. 产品与商业背景：原方案仍缺少需求侧证据

目前“AI 重度开发者最需要”“普通用户愿意安装插件”“少维护足以支持订阅”均是待验证假设。竞品存在只能证明有产品供给，不能证明你们的目标用户愿意切换。

建议首批访谈 5–8 位目标用户，回顾最近一周真实遗漏与人工维护过程。这个人数是探索建议，不代表市场规模估计。需要收集：

- 漏掉的是执行、交付反馈，还是等别人回复？每周出现几次，有什么实际后果？
- 当前靠置顶消息、任务系统、AI 总结或记忆解决到什么程度？
- 愿意授权哪些会话、项目和模型？安装权限是否需要企业管理员？
- 希望继续用原待办系统，还是接受新的事项列表？
- 愿意为减少多少维护时间付费？真实持续使用与付费实验比口头称赞更有价值。

建议补充两个对照：单信源自动提取；两个信源加明确项目/PR 绑定的简单规则。只有复杂跨源推断显著改善这两种基线，额外工程才有依据。

普通用户、配置用户和插件作者是三种不同角色。首版可以坚持公共协议和可安装扩展，同时由官方承担常用插件维护。普通用户的首次价值不应依赖学会 YAML 或找开发者写插件。

单位经济建议按以下公式记录，不预填虚构价格：

月服务成本 = 提取与关联推理 + 核验调用 + 检索/存储/同步 + 失败重试 + 支持维护分摊。

同时记录用户接入时间、每天处理提示的时间及插件故障次数。模型价格低，不代表总维护成本低；本地推理也有硬件与能耗成本。

## 8. 评测设计：补上时间、不可见事实和统计口径

原稿用完整事项链路而非单条文本评测是合适的。建议进一步补充：

**双层标注。** 一层是真实世界发生了什么，由知情者确认；另一层是当时授权数据能支持什么。这样才能区分采集缺口、推理错误与合理保留未知。无法观测的线下完成不能自动算模型漏判。

**按时间前缀回放。** 在每个时间点只给系统当时已经收到的信息，禁止拿最终交付记录辅助早期判断。按用户或项目分离调试集与验收集，避免同一链路变体泄漏。

**自动完成的分母单独报告。** 手动完成、系统建议完成、系统实际自动完成分别统计。持续低覆盖也不能靠高精度被判成功。按风险、信源和事项类型报告，不只看平均值。

**30 条链路只能发现问题。** 例如有 30 次近似独立、可代表目标场景的自动完成，零误判时，二项模型下错误概率的单侧 95% 上界仍约为 9.5%（1−0.05^(1/30)）。30 条链路不一定产生 30 次自动完成，相关性也会削弱此估计。不能据小样本“零错误”宣传接近百分之百可靠。

**增加净收益。** 不只测纠错时间，还测阅读新增事项、处理提醒、配置和排障的时间；同时记录确实避免了哪些遗漏。提醒即便事实准确，也可能没有行动价值。

建议新增回放反例：

| 场景 | 预期 |
| --- | --- |
| 收到请求但没有接受，或只说“我看看” | 根据策略收为请求候选，不臆造明确承诺 |
| 完成测试后继续修改代码 | 旧测试证据不能证明新版本 |
| PR 关闭但未合并 | 不等于合并完成 |
| 同一消息被截图、转发、总结三次 | 不把三份副本当三份独立佐证 |
| 已完成一次性交付，后来出现类似问题 | 判断新事项/复发，不无条件覆盖历史完成 |
| AI 会话停止但仍缺反馈 | 保留未满足的反馈条件 |
| 普通群根消息可读、话题回复遗漏 | 展示覆盖不足，不推断对方未答复 |
| 用户改期后旧事件重放 | 不恢复旧截止时间，其他有效进展仍可更新 |

## 9. 下一轮验证顺序

建议调整为三个短验证，均有明确失败出口：

1. **接入验证**：选定实际桌面平台、一个 AI 工具和飞书测试会话；记录权限、数据样例、话题/编辑/断线覆盖。若核心承诺渠道不可读，先收窄场景，重新评估价值。
2. **事项价值验证**：用原稿约 30 条链路建立时间前缀测试，比较简单基线；先证明重复减少、条件解释有用、人工维护可接受，再扩大自动完成范围。
3. **扩展验证**：让不熟悉核心的人接入一个有实质差异的来源，测试新鉴权、分页或修订能力；如果只是第二份同格式 JSONL，不能充分证明插件扩展性。

竞品试用建议优先覆盖 AirJelly 的完整个人体验、Sally 的自动完成与证据表现、screenpipe 的开发和运维流程。以上优先级来自公开能力与本项目问题的重叠，不是效果排名。

本轮最值得落到方案里的决策问题是：**在你们确实有权持续读取的数据范围内，这个助手能否比现有列表和简单自动化减少真实遗漏与维护时间？** 插件、统一语义和证据模型都应服务于这个结果。
