# D06 模型与输出协议

首版模型调用通过 `packages/model` 的平台无关适配器完成，输入为已授权项目的最小上下文，输出必须经过 JSON Schema 校验后才能进入事项建议流程。模型适配器不得直接写入事项或发送通知；取消、超时和解析失败都返回可记录的错误码。

2026-09-14 更新：以可配置的 Responses / Chat Completions API 为正式事项分析入口，并提供 Codex / Claude Code CLI 适配；协议版本随结构化输出一起记录，确保模型升级后可重放和比较。
