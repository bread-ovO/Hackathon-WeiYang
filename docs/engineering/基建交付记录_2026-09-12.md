# 第一轮工程基建交付

日期：2026-09-12。范围：工程初始化和最小本地基础链路，不包含真实信源或 AI 业务功能。

## 已交付

- pnpm workspace：desktop + contracts/domain/application/storage/connectors/plugin-host/model/evals 八个包。
- React 基础界面、来源未连接状态、真实 SQLite/核心健康页。
- Electron sandbox renderer、类型化最小 preload、IPC 入参与页面/主 frame 检查、自定义本地资源协议；禁止外链弹窗和 webview。
- utilityProcess 核心，三次以内自动重启、请求超时、并发上限和退出清理。
- SQLite v1 迁移：source_instances/source_events/jobs/tasks。WAL、FULL、外键；事件/作业/游标同事务，来源/事件/修订唯一约束。
- JSON Schema Draft-07 输入校验与派生类型。状态类型、归档不销项规则、版本冲突检查；这些不是完整业务状态机。
- 严格 TypeScript、包边界检查、单元与集成测试、桌面端到端测试、macOS GitHub Actions 配置、未签名本地打包配置。

## 实际验证

环境：macOS 26.5 / Darwin 25.5.0，arm64，宿主 Node 22.17.1，pnpm 10.34.5。Electron 44.3.0，React 19.3.0，TypeScript 5.9.3，electron-vite 5.0.0 / Vite 7.3.1，better-sqlite3 13.0.3。运行中查询到 SQLite 3.53.4。精确依赖以 pnpm-lock.yaml 为准。

- `pnpm check` 全部通过：包边界、类型、7 项单元测试、构建、SQLite 集成、1 项覆盖多个断言的桌面测试。
- 数据库测试覆盖：同一事件 10 次仅 1 个事件和作业；修订新建；事务中途模拟失败回滚且游标不前进；数据库重开后数据保持；未知来源拒绝；高版本数据库拒绝启动。
- 桌面测试覆盖：Node 不暴露、bridge 只有 health、真实数据库健康检查、外部弹窗拒绝、核心子进程终止后新进程恢复、来源未连接展示。
- `pnpm package:dir` 成功。启动 release/mac-arm64/多信源事项助手.app 后确认 app.isPackaged=true、核心 ready、schemaVersion=1、事件/作业均为 0。
- 已查看桌面截图，基础页面显示正常。
- `pnpm dev` 实际启动成功，并以开发服务器入口重跑桌面测试通过。开发时允许 React Refresh 的内联前导；生产构建关闭此权限及开发 WebSocket 连接范围。

本机原有 pnpm 全局入口失效，使用 npx 指定项目版本运行，未修改全局配置。首次 npm 二进制下载缓慢，安装重试使用命令级 npm 镜像与已有缓存；未永久修改 registry。

## 未交付与限制

- 无飞书/AI 历史/GitHub 真实连接，无模型请求，无凭据；适配器、插件、模型和评测包当前仅接口。
- 无完整条件、证据、人工决定模型；作业表尚无消费、租约和调度实现；同一来源接收需由调用方串行提交。
- 来源登记不代表授权模块；真实授权、目录和域名权限执行另行开发。
- 数据库尚未加密，只有测试数据；迁移备份/回滚、数据导出删除、签名公证、更新渠道尚未实现。
- 已验证 macOS arm64，未验证 x64、Windows 或 Linux；CI 文件已写入但尚未在远端运行。
- 当前无托盘和退出后服务，macOS 关闭窗口保留应用进程，通过 Dock 可恢复窗口，Cmd+Q 完全退出。
- 包含默认 Electron 图标，未签名 .app 仅用于本机验证，不作为公开发行包。

## 需求表同步口径

E01 已完成；E02/E04/E05/S03/S04 待验收（附本轮实际验证和限制）；S01/S02/U01 进行中。其他条目保持原状态。接口存在不意味着需求完成。

建议下一轮优先确认 D02/D03/D04，随后实现 S01 的编辑/撤回事件语义、S02 完整证据模型、S05 持久作业消费与一种真实来源适配。
